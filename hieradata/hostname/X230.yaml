---
classes:
 - docker
 - vagrant
 - site::packages
 - site::vagrant_plugins
 - virtualbox

docker::docker_users:
 - mt

enhancers:
 - encfs
 - git
 - mlocate
 - pdftk
 - tree
 - vim

vagrant_plugins:
 landrush:
  user: 'mt'
 vagrant-hostmanager:
  user: 'mt'
 vagrant-proxyconf:
  user: 'mt'
 vagrant-triggers:
  user: 'mt'
 vagrant-vbox-snapshot:
  user: 'mt'

virtualbox::version: '5.0'

