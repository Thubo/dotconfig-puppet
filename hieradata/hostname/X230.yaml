---
classes:
 - docker
 - vagrant
 - virtualbox

site::packages:
 - encfs
 - pdftk
 - shutter
 - terminator

docker::docker_users:
 - mt

vagrant_plugins:
 landrush:
  user: 'mt'
 vagrant-hostmanager:
  user: 'mt'
 vagrant-proxyconf:
  user: 'mt'
 vagrant-triggers:
  user: 'mt'
 vagrant-vbox-snapshot:
  user: 'mt'

virtualbox::version: '5.0'

